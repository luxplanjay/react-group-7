import React from 'react';
import Button from './Button';

const Toolbar = () => (
  <div>
    <h2>Toolbar</h2>
    <Button label="Log In" />
    <Button label="Log Out" />
  </div>
);

export default Toolbar;
